const Loader = () => (
    <div style={{ display: 'flex', justifyContent: 'center', alignItems: 'center', height: '400px' }}>
      <div
      style={{
        width: '500px',
        height: '400px',
        backgroundColor: 'gray', // Color for the shape, can be adjusted
        borderRadius: '0px', // Optional: adds rounded corners
      }}
    />
 
    </div>
  );
  
  export default Loader;